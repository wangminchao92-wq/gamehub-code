import React from 'react';
import MainLayout from '@/layouts/MainLayout';
import { ShoppingBag, Tag, Star, ShoppingCart, Search, Filter } from 'lucide-react';

// 游戏商品数据
const games = [
  {
    id: 1,
    title: '赛博朋克2077：往日之影',
    price: 199,
    originalPrice: 299,
    discount: 33,
    platform: 'PC/PS5/Xbox',
    rating: 4.8,
    reviews: '12.4K',
    image: 'https://images.unsplash.com/photo-1550745165-9bc0b252726f?auto=format&fit=crop&w=800&q=80',
    featured: true,
  },
  {
    id: 2,
    title: '艾尔登法环：黄金树之影',
    price: 249,
    originalPrice: 349,
    discount: 29,
    platform: '全平台',
    rating: 4.9,
    reviews: '8.7K',
    image: 'https://images.unsplash.com/photo-1511512578047-dfb367046420?auto=format&fit=crop&w=800&q=80',
    featured: true,
  },
  {
    id: 3,
    title: '博德之门3 豪华版',
    price: 299,
    originalPrice: 399,
    discount: 25,
    platform: 'PC/PS5',
    rating: 4.7,
    reviews: '5.2K',
    image: 'https://images.unsplash.com/photo-1534423861386-85a16f5d13fd?auto=format&fit=crop&w=800&q=80',
    featured: false,
  },
  {
    id: 4,
    title: '最终幻想VII 重生',
    price: 459,
    originalPrice: 499,
    discount: 8,
    platform: 'PS5',
    rating: 4.6,
    reviews: '3.8K',
    image: 'https://images.unsplash.com/photo-1518709268805-4e9042af2176?auto=format&fit=crop&w=800&q=80',
    featured: false,
  },
  {
    id: 5,
    title: '地狱潜者2 超级公民版',
    price: 199,
    originalPrice: 249,
    discount: 20,
    platform: 'PC/PS5',
    rating: 4.5,
    reviews: '6.3K',
    image: 'https://images.unsplash.com/photo-1542751110-97427bbecf20?auto=format&fit=crop&w=800&q=80',
    featured: false,
  },
  {
    id: 6,
    title: '星空 高级版',
    price: 399,
    originalPrice: 499,
    discount: 20,
    platform: 'PC/Xbox',
    rating: 4.3,
    reviews: '4.9K',
    image: 'https://images.unsplash.com/photo-1542751371-adc38448a05e?auto=format&fit=crop&w=800&q=80',
    featured: false,
  },
];

export default function StorePage() {
  return (
    <MainLayout>
      {/* SEO优化 */}
      <SEO
        title="游戏商店 | GameHub - 购买和下载游戏"
        description="在GameHub游戏商店购买和下载最新游戏、DLC和游戏周边产品。享受优惠价格和快速下载。"
        keywords="游戏商店, 购买游戏, 下载游戏, 游戏DLC, 游戏周边"
        canonical="https://gamehub.com/store"
      />
      
      {/* 传统meta标签作为SEO检查备用 */}
      <title>游戏商店 | GameHub - 购买和下载游戏</title>
      <meta name="description" content="在GameHub游戏商店购买和下载最新游戏、DLC和游戏周边产品。享受优惠价格和快速下载。" />
      {/* 页面头部 */}
      <div className="relative bg-gradient-to-r from-gray-900 to-purple-900 py-16">
        <div className="container mx-auto px-4">
          <div className="max-w-4xl">
            <div className="flex items-center mb-4">
              <ShoppingBag className="h-8 w-8 text-purple-400 mr-3" />
              <h1 className="text-4xl md:text-5xl font-bold text-white">游戏商店</h1>
            </div>
            <p className="text-xl text-gray-300 mb-8">
              正版游戏购买、特价优惠、独家内容，一站式游戏购物体验
            </p>
            
            {/* 搜索和筛选 */}
            <div className="flex flex-col md:flex-row gap-4 mb-8">
              <div className="flex-grow relative">
                <Search className="absolute left-4 top-1/2 transform -translate-y-1/2 h-5 w-5 text-gray-400" />
                <input
                  type="text"
                  placeholder="搜索游戏..."
                  className="w-full pl-12 pr-4 py-3 bg-gray-800/50 backdrop-blur-sm border border-gray-700 rounded-lg text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-purple-500 focus:border-transparent"
                />
              </div>
              <button className="flex items-center justify-center px-6 py-3 bg-gray-800/50 backdrop-blur-sm border border-gray-700 rounded-lg text-gray-300 hover:text-white hover:border-purple-500 transition-colors">
                <Filter className="h-5 w-5 mr-2" />
                筛选
              </button>
            </div>
          </div>
        </div>
      </div>

      {/* 主要内容 */}
      <div className="container mx-auto px-4 py-12">
        {/* 特色游戏 */}
        <div className="mb-12">
          <h2 className="text-2xl font-bold text-white mb-6">热门推荐</h2>
          <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
            {games.filter(game => game.featured).map((game) => (
              <div key={game.id} className="group cursor-pointer">
                <div className="relative overflow-hidden rounded-2xl bg-gray-800/50 backdrop-blur-sm border border-gray-700/50">
                  {/* 折扣标签 */}
                  {game.discount > 0 && (
                    <div className="absolute top-4 left-4 z-10">
                      <div className="px-3 py-1 bg-red-500 text-white text-sm font-bold rounded-full">
                        -{game.discount}%
                      </div>
                    </div>
                  )}

                  {/* 游戏图片 */}
                  <div className="relative h-48 overflow-hidden">
                    <div 
                      className="h-full w-full bg-cover bg-center transition-transform duration-700 group-hover:scale-110"
                      style={{ backgroundImage: `url(${game.image})` }}
                    />
                    <div className="absolute inset-0 bg-gradient-to-t from-gray-900 via-gray-900/40 to-transparent" />
                  </div>

                  {/* 游戏信息 */}
                  <div className="p-6">
                    <div className="flex items-center justify-between mb-3">
                      <div className="text-sm text-gray-400">{game.platform}</div>
                      <div className="flex items-center">
                        <Star className="h-4 w-4 text-yellow-400 fill-yellow-400 mr-1" />
                        <span className="text-sm font-bold text-white">{game.rating}</span>
                        <span className="text-sm text-gray-400 ml-1">({game.reviews})</span>
                      </div>
                    </div>

                    <h3 className="text-xl font-bold text-white mb-2 group-hover:text-purple-300 transition-colors">
                      {game.title}
                    </h3>
                    
                    {/* 价格 */}
                    <div className="flex items-center mb-4">
                      <div className="text-2xl font-bold text-white">¥{game.price}</div>
                      {game.originalPrice > game.price && (
                        <>
                          <div className="text-lg text-gray-400 line-through ml-2">¥{game.originalPrice}</div>
                          <div className="text-sm text-red-400 ml-2">立省 ¥{game.originalPrice - game.price}</div>
                        </>
                      )}
                    </div>

                    {/* 购买按钮 */}
                    <button className="w-full py-3 bg-purple-500 text-white rounded-lg hover:bg-purple-600 transition-colors font-medium flex items-center justify-center">
                      <ShoppingCart className="h-5 w-5 mr-2" />
                      加入购物车
                    </button>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>

        {/* 游戏列表 */}
        <div>
          <div className="flex items-center justify-between mb-6">
            <h2 className="text-2xl font-bold text-white">所有游戏</h2>
            <div className="text-sm text-gray-400">
              共 {games.length} 款游戏
            </div>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            {games.map((game) => (
              <div key={game.id} className="group cursor-pointer">
                <div className="bg-gray-800/30 backdrop-blur-sm rounded-xl border border-gray-700/30 hover:border-purple-500/30 transition-colors overflow-hidden">
                  {/* 游戏图片 */}
                  <div className="relative h-48 overflow-hidden">
                    <div 
                      className="h-full w-full bg-cover bg-center transition-transform duration-700 group-hover:scale-110"
                      style={{ backgroundImage: `url(${game.image})` }}
                    />
                    <div className="absolute inset-0 bg-gradient-to-t from-gray-900/60 to-transparent" />
                    
                    {/* 折扣标签 */}
                    {game.discount > 0 && (
                      <div className="absolute top-3 left-3">
                        <div className="px-2 py-1 bg-red-500 text-white text-xs font-bold rounded">
                          -{game.discount}%
                        </div>
                      </div>
                    )}
                  </div>

                  {/* 游戏信息 */}
                  <div className="p-4">
                    <div className="flex items-center justify-between mb-2">
                      <div className="text-xs text-gray-400">{game.platform}</div>
                      <div className="flex items-center">
                        <Star className="h-3 w-3 text-yellow-400 fill-yellow-400 mr-1" />
                        <span className="text-xs font-bold text-white">{game.rating}</span>
                      </div>
                    </div>

                    <h3 className="text-sm font-bold text-white mb-2 line-clamp-1 group-hover:text-purple-300 transition-colors">
                      {game.title}
                    </h3>
                    
                    {/* 价格 */}
                    <div className="flex items-center justify-between mb-3">
                      <div className="text-lg font-bold text-white">¥{game.price}</div>
                      {game.originalPrice > game.price && (
                        <div className="text-xs text-gray-400 line-through">¥{game.originalPrice}</div>
                      )}
                    </div>

                    {/* 购买按钮 */}
                    <button className="w-full py-2 bg-gray-700/50 text-gray-300 rounded-lg hover:bg-purple-500 hover:text-white transition-colors text-sm font-medium">
                      立即购买
                    </button>
                  </div>
                </div>
              </div>
            ))}
          </div>

          {/* 分页 */}
          <div className="flex justify-center mt-12">
            <div className="flex items-center space-x-2">
              <button className="px-4 py-2 bg-gray-800/50 text-gray-300 rounded-lg hover:bg-gray-700/50 transition-colors">
                上一页
              </button>
              {[1, 2, 3, 4, 5].map((page) => (
                <button
                  key={page}
                  className={`px-4 py-2 rounded-lg transition-colors ${
                    page === 1
                      ? 'bg-purple-500 text-white'
                      : 'bg-gray-800/50 text-gray-300 hover:bg-gray-700/50'
                  }`}
                >
                  {page}
                </button>
              ))}
              <button className="px-4 py-2 bg-gray-800/50 text-gray-300 rounded-lg hover:bg-gray-700/50 transition-colors">
                下一页
              </button>
            </div>
          </div>
        </div>
      </div>

      {/* 商店特色 */}
      <div className="bg-gray-800/30 backdrop-blur-sm border-t border-gray-700/30 mt-12">
        <div className="container mx-auto px-4 py-8">
          <h3 className="text-xl font-bold text-white mb-6 text-center">商店特色</h3>
          <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
            {[
              {
                icon: Tag,
                title: '正版保障',
                desc: '所有游戏均为官方正版，享受完整售后服务',
              },
              {
                icon: ShoppingBag,
                title: '优惠活动',
                desc: '定期特价促销，会员专属折扣',
              },
              {
                icon: Star,
                title: '精选推荐',
                desc: '编辑精选游戏，帮你发现优质作品',
              },
            ].map((feature) => {
              const Icon = feature.icon;
              return (
                <div key={feature.title} className="text-center">
                  <div className="w-16 h-16 rounded-full bg-purple-500/20 flex items-center justify-center mx-auto mb-4">
                    <Icon className="h-8 w-8 text-purple-400" />
                  </div>
                  <div className="text-lg font-bold text-white mb-2">{feature.title}</div>
                  <div className="text-sm text-gray-400">{feature.desc}</div>
                </div>
              );
            })}
          </div>
        </div>
      </div>
    </MainLayout>
  );
}